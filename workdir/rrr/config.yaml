tag: rrr
name:

time_limit:
# in s.... problem with units
memory_limit:
# in MB..... problem with units

subtasks: 1
number_of_generated_testcases_per_subtask: []

checker: default_checker.cpp
checker_bin:

model_solution: rrr.cpp
model_solution_bin:

trusted_brute_force_solution:

generator: gen.cpp
generator_bin:

input_verifier: inver.cpp
input_verifier_bin:

other_solutions:
- name:
  program:
  program_bin:
  pass_subtasks: []
